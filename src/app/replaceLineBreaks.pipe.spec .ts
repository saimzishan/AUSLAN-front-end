import { ReplaceLineBreaks } from './replaceLineBreaks.pipe';

describe('ReplaceLineBreaks', () => {
  it('create an instance', () => {
    const pipe = new ReplaceLineBreaks();
    expect(pipe).toBeTruthy();
  });
});
